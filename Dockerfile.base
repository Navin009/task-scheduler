FROM rust:1.70-slim-bullseye as builder

WORKDIR /usr/src/app
RUN apt-get update && apt-get install -y pkg-config libssl-dev

COPY . .

# This is a base image, actual build commands will be in individual Dockerfiles